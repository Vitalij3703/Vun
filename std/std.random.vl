if(!isset(__STD_RANDOM)){

bool __STD_RANDOM = true;

if(!isset(__STD_LIMITS)){
    include("std.limits.vl")
}

func rand(){
    int n = (1103515245 * seed + 12345) % INT_MAX;
    return n;
}

func randrange(int start, int stop){
    if (stop <= start){
        errorln("From std.math.vl, randrange():\n\tStarting value aboove stopping value");
    }
    int span = stop - start;
    int idx = rand() % span;
    return start + idx;
}

}